<%= form_for @post do |form| %>
  <%= form.label :message %>
      db-change-add-relations
  <%= form.text_area :message %>
  <%= form.hidden_field :user_id, :value => current_user.id %>
  <%= form.submit "Submit" %>
<% end %>
