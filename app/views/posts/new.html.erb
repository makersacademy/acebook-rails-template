
<%= form_for @post do |form| %>
  <%= form.label :message %>
  <%= form.text_area(:message, :size => "60x10") %>
  <%= hidden_field(:post, :recipient_id, value: @current_user.id) %>
  <%= hidden_field(:post, :post_type, value: 'public') %>
  <%= form.submit "Submit" %>
<% end %>
