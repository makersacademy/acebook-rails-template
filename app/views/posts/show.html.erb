<%= render 'shared/nav' %>

<%= @post.text %>
 <br>
 <br>
Leave a comment:
<br>
<%= form_for @comment, url: create_comment_path([:post_id]) do |f| %>

  <%= f.text_area :text %>
  <%= f.hidden_field :post_id, :value => @post.id %>
  <br>
  <%= f.submit 'Comment' %>
<% end %>
<br>
<br>
