<%= simple_format(@post.message) %><br>
Created <%= @post.created_at.strftime("on %d/%m/%Y at %I:%M%p") %>
<%= link_to 'Delete', post_path, method: :delete, data: {confirm: "Are you sure?"}  %>

<% if @post.edit_timer? %>
  <%= link_to 'Edit', edit_post_path %>
<%end%>

<% if flash[:error] %>
   <div><%= flash[:error] %></div>
 <% end %>
