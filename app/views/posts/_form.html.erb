<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :post %>
    <%= form.text_area :post, id: :post_post%>
  </div>

  <div class="field">
    <%= form.number_field :user_id, value: current_user.id, type: :hidden%>
  </div>

  <div class="field">
    <%= form.label :username %>
    <%= form.text_area :username, value: current_user.username%>
  </div>
 

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
