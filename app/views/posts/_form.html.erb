
<%= form_with model: @post, local: true do |form| %>

  <%= hidden_field_tag 'post[user_id]', current_user.id %>
  <%= form.label :message, "Post a message" %>
  <br>
  <%= form.text_area :message %>
  <br>
  <%= form.submit "Submit" %>
<% end %>
