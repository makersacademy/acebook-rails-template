
var postId = "<%= @post.id %>"
console.log(postId);
var thisPost = "<%= @post %>"
console.log(thisPost);
var postFave = "<%= @post.favourited %>"
console.log(postFave);

function toggleLikeButton(postId, innerHTML) {
  let likeButton = document.querySelector(`#like-button-${postId}`)
  let likeCount = document.querySelector(`#like-count-${postId}`)
  if(likeButton.style.color === "grey"){
    likeButton.style.color = "blue"
  } else {
    likeButton.style.color = "grey"
  }
}

toggleLikeButton(postId)
