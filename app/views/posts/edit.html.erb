<h1>Edit message</h1>

<%= form_with(model: @post, local: true) do |form| %>
  <p>
    <%= form.label :message %><br>
    <%= form.text_area :message, :value => Post.find(params[:id]).message, size: "30x30" %>
  </p>

  <p><%= form.submit %> <%= link_to 'Back', posts_path %></p>
<%end%>

