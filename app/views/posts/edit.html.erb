<h1>Edit post</h1>

<%= form_with(model: @post, local: true) do |form| %>
  <%= form.label :message %>
  <%= form.text_field :message %>

  <%= form.submit "Submit" %>
<% end %>

<%= link_to 'Back', posts_path %>
