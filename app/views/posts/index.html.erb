<% @posts.each do |post| %>
  <div>
    <p><%= post.message %></p>
    <p><%= post.created_at.strftime("%m/%d/%Y/%H/%M/%S") %></p>
  </div>
<% end %>

<%= link_to new_post_path do %>
  Write a New post
<% end %>
