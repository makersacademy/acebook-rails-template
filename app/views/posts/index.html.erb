<% @posts.each do |post| %>
  <p><%= post.message %>
  <caption><%= link_to post.user == nil ? "anonymous" : post.user %></caption>
  <%= link_to edit_post_path(id: post.id) do %>Edit Post<% end %>
  <%= link_to "Delete", post, method: :delete %>
</p>

<% end %>

<%= link_to new_post_path do %>
New post
<% end %>