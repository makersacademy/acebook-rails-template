<% @posts.each do |post| %>
  <p><%= post.message %> <%= button_to "Delete Post", { :action => 'destroy', :id => post.id }, :method => :delete %> <%= button_to "Update Post", { :action => 'update', :id => post.id }, :method => :patch %></p> 
<% end %>

<%= link_to new_post_path do %>
  New post
<% end %>
