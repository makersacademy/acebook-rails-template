<%= link_to('Sign out', destroy_user_session_path, method: :delete) %>

<% @posts.each do |post| %>
  <p><%= post.message %></p>
<% end %>

<%= button_to "New posts", new_post_path, :method => :get %>
