<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.0/css/bootstrap.min.css" integrity="sha384-9gVQ4dYFwwWSjIDZnLEWnxCjeSWFphJiwGPXr1jddIhOegiu1FwO5qRGvFXOdJZ4" crossorigin="anonymous">
    <style>
      body {
        padding-top: 50px;
        padding-left: 50px;
        padding-right: 50px;
      }
      .list-group {
        padding-bottom: 20px;
      }
      .likes-count {
        text-align: left;
      }
      .comments-count {
        text-align: right;
      }
      .counters {
        display: flex;
        flex-direction: row;
        justify-content: flex-start;
      }
    </style>
  <title></title>
  </head>
  <body>
    <p class="navbar-text pull-right">
      <% if user_signed_in? %>
        Logged in as <strong><%= current_user.identifier %></strong>.
        <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> |
        <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
      <% else %>
        <%= link_to "Sign up", new_user_registration_path, :class => 'navbar-link'  %> |
        <%= link_to "Login", new_user_session_path, :class => 'navbar-link'  %>
      <% end %>
    </p>
    <h1>Acebook</h1>
    <%= link_to new_post_path do %>
      <p>New post</p>
    <% end %>
    <% @posts.time_sort_all.each_with_index do |post, i| %>
      <div id="<%="post_#{i}"%>" class="list-group">
        <div class="list-group-item">
        <div id="<%="post_username_#{i}"%>">
          <%= post.author %>
        </div>
        <div class="small"><%= post.created_at_timestring %></div>
          <div id="<%="post_message_#{i}"%>">
            <h5><%= simple_format(Rinku.auto_link(post.message)) %></h5>
          </div>
          <% if post.image.file? %>
            <%= image_tag post.image.url, :style => "max-height: 500px; max-width: 500px;" %>
          <% end %>
          <span class="counters">
            <% pre_like = post.find_like(current_user) %>
            <% if pre_like %>
              <%= button_to 'Unlike', post_like_path(post, pre_like) + "#post_#{i}", method: :delete %>
            <% else %>
              <%= button_to 'Like', post_likes_path(post) + "#post_#{i}", method: :post %>
            <% end %>
            <%= post.like_count_string %>
            <%= post.comment_count_string %>
          </span>
          <span>
            <% if post.owner?(current_user) %>
              <%= link_to 'Edit', edit_post_path(post) %>
              <%= link_to 'Delete', post_path(post),
                method: :delete,
                data: { confirm: 'Are you sure?' } %>
            <% end %>
            <%= link_to 'View Comments', post_path(post) %>
          </span>
        </div>
      </div>
    <% end %>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.0/js/bootstrap.min.js" integrity="sha384-uefMccjFJAIv6A+rW+L4AHf99KvxDjWSu1z9VI8SKNVmz4sk7buKt/6v9KI65qnm" crossorigin="anonymous"></script>
  </body>
</html>
