<% @posts.each do |post| %>
<div>
  <p><%= (post.created_at).strftime("%l:%M %p - %e %b '%y")   %>&nbsp;&nbsp;&nbsp;
    <%= post.message %></p>
</div>
<% end %>

<%= link_to new_post_path do %>
  New post
<% end %>
