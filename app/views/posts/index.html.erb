<!DOCTYPE html>
<html>
    <head>
      <meta charset="utf-8">
      <meta http-equiv="X-UA-Compatible" content="IE=edge">
      <meta name="viewport" content="width=device-width, initial-scale=1">
      <link rel="stylesheet" type="text/css" href="style.css" />
      <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
      <link href="https://fonts.googleapis.com/css?family=Nanum+Gothic&display=swap" rel="stylesheet">
    </head>

      <body>

        <nav class="navbar-acebook navbar navbar-expand-md" style="border-radius: 0px !important;">
          <div class="navbar-brand-logo" <span>&#9824;</span>cebook</div>
          <div class="collapse navbar-collapse" id="main-navigation">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/login">Home</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/logout">Log out</a>
              </li>
            </ul>
          </div>
        </nav>

        <div class="myDiv">
        <div class="bg">

        <!-- <header class="page-header header container-fluid">
        </header> -->

        <h1 class="welcome">Welcome to <span>&#9824;</span>cebook</h1>

        <%= flash[:error]%>


        <%= form_for( :post, url: posts_path) do |form| %>
          <%= form.label :message %>
          <%= form.text_area :message %>
          <%= form.submit "Submit" %>

        <% end %>

        <% @posts.reverse_each do |post| %>
      <p><%= User.find(post.user_id).username %> <%= best_in_place post, :message %>
      (posted <%= time_ago_in_words(post.created_at)%> ago)
        <%= link_to edit_post_path(post.id) do %>
          Edit
        <% end %></p>
        <%=link_to 'Delete', post, method: :delete, data: { confirm: "Are you sure?"} %>

        <% end %>
        <div id="all-posts">
        <% @posts.reverse_each do |post| %>
        <div class="post">
          <div class="dropdown">
            <button class="dropbtn">...</button>
            <div class="dropdown-content">
              <a> <% link_to edit_post_path(post.id) do %>
              Edit
              <% end %> </a>
              <a> <%=link_to 'Delete', post, method: :delete, data: { confirm: "Are you sure?"} %> </a>
            </div>
          </div>
            <div class="post-body">
              <h2 class="post-user"><%= User.find(post.user_id).username %> </h2>
              <p class="posts-time">posted <%= time_ago_in_words(post.created_at)%> ago</p>
              <p class="post-text"> <%= post.message %> </p>
            </div>
            <br>
        </div>
        <% end %>

        </div>

    </div>
    <%= link_to "Log out",session_path(session), method: :delete %>
    </div>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
    <script src="../main.js"></script>

  </body>
</html>
