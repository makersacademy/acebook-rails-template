<% @posts.each do |post| %>
  <div class="card">
  <p><%= post.message %><br>
  <%= "iluvcatz123" %><br>
  <%= post.created_at.strftime("%m/%d/%Y, %H:%M") %>
  </p>
  </div>
<% end %>

<%= link_to new_post_path do %>
  New post
<% end %>
