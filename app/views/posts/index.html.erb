<% @posts.each do |post| %>
  <p><%= post.message %></p>
  <p><%= post.created_at.strftime("%I:%M %p") %></p>
  <%= link_to edit_post_path(@post) do %>
    Edit
  <% end %>
<% end %>

<%= link_to new_post_path do %>
  New post
<% end %>
