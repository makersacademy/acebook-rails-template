<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>DART</title>

  </head>
  <body>
    <div id="new-post">
      <%= link_to new_post_path do %>
        New post
      <% end %>
    </div>
    <br>
    <div id="all-posts">
      <% @posts.each do |post| %>
        <p><%= post.message %></p>
        <p><%= post.created_at.to_s.split(" ")[0] %>
        <%= post.created_at.to_s.split(" ")[1][0...-3] %></p>

        <% post.comments.each do |comment|%>
          <p><%= comment.body%></p>
        <% end %>
        <%= form_with(model: [ post, post.comments.build], local: true) do |form| %>
          <%= form.text_area :body, cols: "50", rows: "5" %>
          <br>
          <%=form.submit "Comment"%>
          <br>
          <br>
          <br>
          <br>
        <% end %>
      <% end %>

    </div>
  </body>
</html>
