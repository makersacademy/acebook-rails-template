<% @posts.each do |post| %>
  <p><%= post.created_at.to_date%> <%= post.created_at.strftime("%k:%M") %></p>
  
  <p><%= post.message %></p>
<% end %>

<%= link_to new_post_path do %>
  New post
<% end %>
