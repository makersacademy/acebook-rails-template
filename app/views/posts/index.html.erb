<% @posts.each do |post| %>
  <p><%= post.message %></p>
  <p> created by <%= post.user_id %>
  <%= link_to "Edit Post", edit_post_path(post) %></p>
  <%= link_to "Delete Post", post, method: :delete, data: {confirm: "Are you sure?"} %>
<% end %>
<br>
<%= link_to "New post", new_post_path %>
