<h1> acebook </h1>

<ul>
  <% @posts.each do |post| %>
    <li>
      <%= link_to post.content, post%>
    </li>
  <% end %>
</ul>

<%= link_to 'Sign out', destroy_user_session_path,
              method: :delete,
              data: { confirm: 'Are you sure?' } %>
