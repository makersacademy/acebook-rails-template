<% provide(:title, 'Users') %>
<h2 class="page-title">Users</h2>

<ul>
    <% @users.each do |user| %>
      <li><a href="<%=user.id%>"><%= gravatar_for user %><%= user.name %></a></li>
    <% end %>
</ul>
