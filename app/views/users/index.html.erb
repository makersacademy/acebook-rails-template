<div class='users'>
    <% @users.each do |user| %>
        <div class='user'>
            <p>
            <%= user.email %>
            <%= link_to '/friendships', :friend_id => user.id, :method => :post do %>
                Add Friend
            <% end %>
            <p>
        </div>        
    <% end %>
</div>


// , friendships_path(:friend_id => user.id), method=> :post