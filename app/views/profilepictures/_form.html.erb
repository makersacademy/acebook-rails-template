<div class="mx-auto" style="width: 300px;">

  <%= form_with(model: profilepicture, local: true) do |form| %>
    <% if profilepicture.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(profilepicture.errors.count, "error") %> prohibited this profile picture from being saved:</h2>

        <ul>
        <% profilepicture.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
  <div class="form-group">
    <div class="field">
      <%= form.label :caption %>
      <%= form.text_area :caption, class: "form-control text-center" %>
    </div>
</div>
<div class="form-group">
    <div class="field">
      <%= form.label :image %>
      <%= form.file_field :image, class: "form-control" %>
    </div>
</div>
    <div class="actions">
      <%= form.submit "Upload Profile Picture", class: "btn btn-primary"%>
    </div>
  <% end %>

</div>