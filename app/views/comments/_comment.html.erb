<% if comment.comment_text != nil %>
<% time = comment.created_at%>
<p><%= comment.comment_text %> - <%= time_ago_in_words(comment.created_at || Time.now) %> - <%= get_comment_email(comment.user_id) %></p>
<% end %>
