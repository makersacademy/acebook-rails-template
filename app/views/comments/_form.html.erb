<%= form_with(model: [ @post, @post.comments.build ], local: true) do |form| %>
<p>
  <%= hidden_field_tag 'comment[commenter]', current_user.username %>
  <%= form.label :body %><br>
  <%= form.text_area :body %>
</p>
<p>
  <%= form.submit %>
</p>
<% end %>

<%= link_to 'Back', posts_path %>
