<h1>Comment page</h1>

<%= form_for @comment do |form| %>
  <%= form.label :comment %>
  <%= form.text_field :comment %>
  <%= form.hidden_field :post_id, value: "#{params[:post_id]}" %>

  <%= form.submit "Submit" %>
<% end %>
