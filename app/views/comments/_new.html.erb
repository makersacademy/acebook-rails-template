<%= form_for [post, post.comments.new] do |form| %>
  <%= form.text_field :body, class: "comment-form-textarea", placeholder: "Take the bait, leave a comment" %>
  <%= form.submit 'Add comment', class: "comment-form-submit" %>
<% end %>