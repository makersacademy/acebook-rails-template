<!DOCTYPE html>
<html>
  <head>
    <title>Acebook</title>
    <%= csrf_meta_tags %>

    <!--<link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    and replace-->
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <link rel="stylesheet" href="../assets/stylesheets/style.css">
    <link href="https://fonts.googleapis.com/css?family=Fira+Sans|Open+Sans:300|Raleway:100,600,700,900&display=swap"
        rel="stylesheet">
  </head>
  <body>
    <div id="grid">
      <%# <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container">
     <a id="logo" class="navbar-brand" href="/">Acebook</a>

        <a class="navbar-brand" href="/">AceBook</a>

        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
              <a class="nav-link" href="/ideas">Comments</a>
            </li>
            <li class="nav-item">
               <a class="nav-link" href="/pages/info">About</a>
            </li>
          </ul>
          <p class="navbar-text float-right"> %>
      <div id="logo-cont">
        <div id="logo">
          <p>Acebook</p>
          <%# <div id="sign-up"> %>
            <%# <a href="#">Sign Up</a> %>
            <%# <%=# link_to "Sign up", new_user_registration_path, :class => 'navbar-link'  %>
          <%# </div> %>
        </div>
      </div>

      <div id="container">
        <% if user_signed_in? %>
          Logged in as <strong><%= current_user.email %></strong>.
          <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> |
          <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>
          <%= image_tag current_user.gravatar_url, :class => "gravatar" %>
        <%# else %>
          <%= link_to "Sign up", new_user_registration_path, :class => 'sign_up'  %>  |
          <%= link_to "Login", new_user_session_path, :class => 'login_btn'  %>
        <% end %>
        <%# </p> %>
        <%# </div> %>
        <%# </div> %>
        <%# </nav> %>
        <% if notice %>
          <p class="alert alert-success"><%= notice%></p> 
        <% end %>
        <% if alert %>
          <p class="alert alert-danger"><%= alert %></p>
        <% end %>
        <div class="container">
          <%= yield %>
        </div>
      </div>
      <footer>
        <div class="container">
          <p id="cop">Runtime TError &copy; <%= Time.now.year %></p>
        </div>
      </footer>
    </div>
    <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.3/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
  </body>
</html>
