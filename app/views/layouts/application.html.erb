<!DOCTYPE html>
<html lang="en">
<head>

  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>Acebook</title>
  <%= csrf_meta_tags %>

  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
</head>

<body>

  <header class= "container">
    <nav class="navbar navbar-expand-lg navbar-dark bg-light">
      <a class="navbar-brand">Acebook</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarText">

        <ul class="navbar-nav mr-auto">

          <% if signed_in? %>
          <li class="nav-item active"><%= link_to "Posts", posts_path%></li>
          <li class="nav-item active"><%= link_to "New post", new_post_path%></li>
          <li class="nav-item active"><%= link_to "Update Profile", edit_user_path(current_user.id)%></li>
          <li class="nav-item active"><%= link_to "View photo albums", albums_path%></li>
          <% else %>
          <li class="nav-item active"><%= link_to 'Sign in', sign_in_path %></li>
          <% end %>
        </ul>
        <div style="padding-left: 10px;">
          <% if signed_in? %>
          Signed in as: <%= current_user.email %>
          <%= button_to 'Sign out', sign_out_path, method: :delete%>
          <%end%>
        </div>

      </nav>

        <div class="jumbotron text-center">
          <h1>Acebook</h1>
          <p>Welcome to Acebook</p>
           <%# <img src="images/makers.jpg">  %>
        </div>
    </header>


    <section class= "container" >
      <div id="flash">
        <% flash.each do |key, value| %>
        <div class="flash <%= key %>"><%= value %></div>
        <% end %>
      </div>
      <%= yield %>
    </section>

  </body>
  </html>
