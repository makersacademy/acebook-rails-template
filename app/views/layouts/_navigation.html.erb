<nav class="navbar navbar-inverse navbar-static-top" style="height:20px">
  <div class=".col-container-fluid"></div>
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
      </button>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li>
          <%= image_tag("wookie.png", size: '40x40', class: "img-responsive center-block") %>
        </li>
        
        <li><a href="/users/0/posts" style="font-size:20px">wookiebook</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right" style="position:absolute; right:20px; font-size:16px">
        <% if session[:user_id] %>
          <li><%= link_to 'Logout', logout_path, :method => :delete %></li>
        <% end %>
        <% unless session[:user_id] %>
          <li><%= link_to 'Signup', signup_path, :method => :get %></li>
          <li><%= link_to 'Login', login_path, :method => :get %></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>