<%= form_for Post.new do |form| %>
  <%= form.label id: 'form-label' do %>
    <mark class='yellow'>Post</mark><mark class='blue'>.new</mark>
  <% end %>
  <%= form.text_field :message, placeholder: 'message:' %>

  <%= form.submit "Submit", id: 'form_submit_button' %>
<% end %>
