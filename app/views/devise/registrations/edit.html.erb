<h2>Edit <%= resource_name.to_s.humanize %></h2>
<br>
Please enter your details below if you wish to change your user details
<br>
<br>
<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
  <%= render "devise/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :email %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email",class: "form-control",placeholder:"Please enter your email" %>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="mb3">
  <%= f.label :username %><br />
  <%= f.text_field :username, autofocus: true, autocomplete: "username",class: "form-control", placeholder: "Enter your username here" %>
  </div>

  <div class="field">
    <%= f.label :password %> <i>(leave blank if you don't want to change it)<em> <%= @minimum_password_length %> - 10 characters</em></i><br />
    <%= f.password_field :password, autocomplete: "new-password",class: "form-control", placeholder: "Enter a new password here" %>
    <% if @minimum_password_length %>
  <div class="field">
    <%= f.label :password_confirmation %><br />
    <%= f.password_field :password_confirmation, autocomplete: "new-password",class: "form-control", placeholder: "Enter your previous password here" %>
  </div>
 <br>
    <% end %>
  </div>
  If you wish to continue with the changes, please confirm your previous password below:
  <br>
  <br>

  <div class="field">
    <%= f.label :current_password %> <i>(we need your current password to confirm your changes)</i><br />
    <%= f.password_field :current_password, autocomplete: "current-password",class: "form-control", placeholder: "Confirm your password to continue with your changes" %>
  </div>
<br>
  <div class="actions">
    <%= f.submit "Update",class:'btn btn-success' %>
  </div>
<% end %>
<%= link_to "Back", :back,class:'btn btn-light' %>
<br>
<br>
<h3>Cancel my account</h3>

<p>Unhappy? Please don't leave us 😭
<%= button_to "Cancel my account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete,class:'btn btn-danger' %></p>
<%= link_to "Back", :back,class:'btn btn-light' %>