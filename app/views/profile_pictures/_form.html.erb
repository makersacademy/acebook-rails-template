<%= form_with(model: profile_picture, local: true) do |form| %>
  <% if profile_picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(profile_picture.errors.count, "error") %> prohibited this profile_picture from being saved:</h2>

      <ul>
      <% profile_picture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
