
 <%= link_to 'New Album', new_album_path %>
<div>
    Albums:
</div>
<% @albums.reverse.each do |album| %>
    <p>
        <%= album.title %>
        <%# <%= image_tag(album.images, style:'width:5% ') unless album.images.nil? %> 
    </p>
    <%end%>
<%= link_to 'Back', posts_path %>