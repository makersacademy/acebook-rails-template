<span class='parent-likes'>
  <span class="like_count" <%=post.id%>>
  <%= post.get_likes.size %> &#128077
  </span>
  </span><a>
    <% if current_user.liked? post %>
      <div> <%= binding.pry; link_to like_post_path(post, 'unlike'), id: "preached-btn", method: :put, remote: :true do %>You like this post.</div>
    <% end%>
    <% else %>
      <div> <%= link_to like_post_path(post, 'like'), id: "preach-btn", method: :put, remote: :true do %>thumzup?</div>
    <% end%>

    <% end%>
        --------------------------
    </a></span>
