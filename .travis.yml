language: ruby
rvm:
  - 2.5.0  

install: 
  - gem uninstall bundler
  - gem install bundler --version '1.17.2'
  - bundle install

services: postgresql
 
before_script:
  - rails db:create
  - rails db:migrate 

script:
  - bundle exec rake    
  - bundle exec rspec