
Authorization: uF1ENl_b529seMUwCY2UjA

install:
  - bundle update --bundler
  - bundle install
services:
  - postgresql

script:
  - bundle exec bin/rails db:create
  - bundle exec bin/rails db:migrate
  - bundle exec bin/rails db:environment:set RAILS_ENV=test
  - bundle exec rspec
  - bundle exec rubocop

deploy:
  provider: heroku:git
  api_key: 657db9bb-d791-45d1-9c3c-3bb3725d5a7a
  app: fierce-plains-18412
  edge: true
  run: rake db:migrate
  