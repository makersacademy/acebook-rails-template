language: ruby
rvm:
  - 2.5.0

before_install:
  - gem update --system
  - gem install bundler

services:
  - postgresql

script:
  - bundle install 
  # - jobs=3 
  # - retry=3
  - bundle exec bin/rails db:create
  - bundle exec bin/rails db:migrate
  # — bundle exec rspec
