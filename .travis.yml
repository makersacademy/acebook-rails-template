language: ruby

# Services in use
services: 
- postgreSQL
 
before_install:
-gem update —system
-gem install bundler
# set up database
script:
 — bundle exec rake db:create
 — bundle exec rake db:migrate
 — bundle exec rspec spec

