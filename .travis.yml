language: ruby
rvm:
  - 2.5.0
before_script:
  - psql -c 'Create databast for travis_ci_test;' -U postgres
script:
  - bundle exec rake db:create
  - bundle exec rake db:migrate
  - bundle exec rspec
