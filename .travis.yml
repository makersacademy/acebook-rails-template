language: ruby
rvm:
- 2.5.0
- jruby-19mode
- rbx-2

before_install:
  - gem update --system
  - gem install bundler

before_script:
  - bundle exec rake db:migrate
  - bundle exec rake db:test:prepare

script:  
 — bundle exec rspec

