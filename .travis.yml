language: ruby

rvm:
- 2.7.0

services: 
- postgresql

cache: bundler
 
before_install:
- gem update —system
- gem install bundler

script:
- bundle install
- bundle exec rake db:create
- bundle exec rake db:migrate
- bundle exec rspec spec