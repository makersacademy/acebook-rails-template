language: ruby
rvm:
- 2.0.0
- 2.2.2
- 1.9.3
- jruby-19mode
- rbx-2

  # before_install:
  #   - gem update --system
  #   - gem update bundler

  script:  
  — bundle exec rake db:create
  — bundle exec rake db:migrate
  — bundle exec rspec

